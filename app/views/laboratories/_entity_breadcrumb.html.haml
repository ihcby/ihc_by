%ol.breadcrumb
  %li= link_to @account.name, @account
  %li= link_to t('pages.home.links.laboratories.title'), account_laboratories_path(@account)
  %li.active= @laboratory.persisted? ? @laboratory.name : t('pages.laboratory.links.laboratories_list.new')

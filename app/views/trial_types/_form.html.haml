= render partial: 'entity_breadcrumb'
.row
  .col-lg-6.col-lg-offset-3
    .well
      = simple_form_for([@account, @laboratory, @trial_type]) do |f|
        %legend
          = form_title(@trial_type)

        = f.input :name
        = f.button :submit, t('common.actions.save')

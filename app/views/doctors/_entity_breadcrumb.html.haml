%ol.breadcrumb
  %li= link_to @account.name, @account
  %li= link_to t('pages.home.links.doctors.title'), account_doctors_path(@account)
  %li.active= @doctor.persisted? ? @doctor.name : t('pages.doctors.links.doctors_list.new')
